<nav class="bg-white border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      
      <div class="flex items-center">
        <%= link_to "🏠 AirbnbClone", root_path, class: "font-bold text-2xl text-blue-600" %>
      </div>

      <div class="flex items-center space-x-6">
        <% if user_signed_in? %>
          <span class="text-sm font-medium text-gray-500"><%= current_user.email %></span>
          <%= link_to "Mis Viajes", mis_viajes_path, class: "text-sm font-medium text-gray-700 hover:text-blue-600" %>
          <%= link_to "Mis Propiedades", mis_propiedades_path, class: "text-sm font-medium text-gray-700 hover:text-blue-600" %>
          <%= link_to "Publicar Propiedad", new_property_path, class: "text-sm font-medium text-gray-700 hover:text-blue-600" %>
          
          <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, class: "text-sm font-medium text-red-600 hover:text-red-800 bg-transparent cursor-pointer" %>
        <% else %>
          <%= link_to "Iniciar sesión", new_user_session_path, class: "text-sm font-medium text-gray-700 hover:text-blue-600" %>
          <%= link_to "Registrarse", new_user_registration_path, class: "text-sm font-medium text-white bg-blue-600 px-4 py-2 rounded-lg hover:bg-blue-700 transition" %>
        <% end %>
      </div>

    </div>
  </div>
</nav>